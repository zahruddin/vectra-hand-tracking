services:
  vectra-server:
    build: .
    container_name: vectra-server
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - SECRET_KEY=vectra_sdmuhla_key
      - PYTHONUNBUFFERED=1
    volumes:
      - ./calibration.json:/app/calibration.json
