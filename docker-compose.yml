services:
  vectra-server:
    build: .
    container_name: vectra-server
    restart: unless-stopped
    ports:
      - "5000:5000"
    volumes:
      - ./calibration.json:/app/calibration.json
    environment:
      - PYTHONUNBUFFERED=1
      - PORT=5000
